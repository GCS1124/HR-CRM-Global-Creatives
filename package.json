{
  "name": "hr-crm-global-creatives",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run dev --prefix client -- --host 0.0.0.0 --port 5173",
    "build": "npm run build --prefix client",
    "lint": "npm run lint --prefix client",
    "setup:admin": "node supabase/bootstrap-admin.mjs"
  }
}
